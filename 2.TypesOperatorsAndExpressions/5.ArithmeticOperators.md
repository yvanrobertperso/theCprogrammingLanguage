# Arithmetic Operators

The binary arithmetic operators are +,-,*,/ and the modulo operator %.
Integer division truncates any fractional parts.
The % operator cannot apply to `float` or `double`.
The binary + and - have the same precedence, which is lower than the precedence of *,/ and %, which is in turn lower than unary + and -.
