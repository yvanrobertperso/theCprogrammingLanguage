# Arguments - Call by value


In C, all function arguments are passed "by values". 
This means that the called function is given the values of its arguments in temporary variables rather than the originals.

When the name of an array is used as an argument, the value passed to the function is the **location or address** at the beginning of the array - there is no copying of array elements.  
