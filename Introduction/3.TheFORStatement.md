# The FOR Statement

See the program `FahrWithFor.c`.

This program produces the same results as with `while`, but uses `for`.

Most variables are eliminated; only `fahr` remains. The Celsius calculation is done directly in `printf`.

The choice between `while` and `for` is arbitrary, based on clarity. `for` is more compact when initialization and increment are related.
